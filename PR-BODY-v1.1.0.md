# Homebrew Formula PR Body — v1.1.0

**PR title:** `nself 1.1.0`

---

## Summary

Updates the `nself` formula to v1.1.0.

## Changes

- `url`: updated to `https://github.com/nself-org/cli/archive/refs/tags/v1.1.0.tar.gz`
- `sha256`: _(populated by goreleaser release automation — do not set manually)_
- `version`: `1.1.0`

## Bottle block

The bottle block is regenerated by CI after the formula PR merges. The `brew test-bot` workflow handles this automatically.

## Verification

After formula lands:

```bash
brew update
brew install nself
nself version
# expected: nself version 1.1.0 (or v1.1.0)
```

## Notes

- This formula tracks CLI exactly (lockstep per nSelf version policy)
- No dependency changes vs v1.0.16
- `goreleaser` dispatches this PR automatically on CLI tag publish via `.github/workflows/release.yml` in the CLI repo

## Linked release

https://github.com/nself-org/cli/releases/tag/v1.1.0
